# Server Configuration
PORT=8912
LOG_LEVEL=info

# Embeddings API Configuration
# For local development (when embeddings-api runs on host)
EMBEDDINGS_API_HOST=localhost
EMBEDDINGS_API_PORT=6002

# For Docker (when embeddings-api runs in container)
# EMBEDDINGS_API_HOST=embeddings-api
# EMBEDDINGS_API_PORT=8001

EMBEDDINGS_API_TIMEOUT=60000

# Codebase paths (configurable defaults)
DEFAULT_CODEBASE_PATH=/workspace
